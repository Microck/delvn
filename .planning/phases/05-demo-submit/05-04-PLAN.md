---
phase: 05-demo-submit
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - demo/recording_checklist.md
  - demo/submission_checklist.md
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Recording checklist live steps invoke true live mode (`--live`) with demo config"
    - "Submission checklist includes a final video section (hosted URL preferred; repo video optional)"
  artifacts:
    - path: "demo/recording_checklist.md"
      provides: "Correct live command for recording runbook"
    - path: "demo/submission_checklist.md"
      provides: "Submission sanity checks including final video section"
  key_links:
    - from: "demo/recording_checklist.md"
      to: "demo/run_demo.py"
      via: "live command in recording steps"
      pattern: "--live --config demo/config.yaml"
    - from: "demo/submission_checklist.md"
      to: "(hosted video URL)"
      via: "final packaging verification"
      pattern: "Hosted video URL:"
---

<objective>
Close Phase 05 verification gaps by fixing the live recording command and tightening the submission checklist's video section.

Purpose: Make the demo capture workflow correct and non-blocking for automated execution.
Output: Updated recording/submission checklists.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-demo-submit/05-demo-submit-VERIFICATION.md
@.planning/phases/05-demo-submit/05-03-SUMMARY.md
@demo/run_demo.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix recording checklist live command and tighten video submission checks</name>
  <files>demo/recording_checklist.md, demo/submission_checklist.md</files>
  <action>
Update `demo/recording_checklist.md` so every live execution instruction uses the real live invocation:
- `python3 demo/run_demo.py --live --config demo/config.yaml`

Also update `demo/submission_checklist.md` to include an explicit final video section:
- Hosted video URL (preferred for submissions where large binaries are not committed)
- Optional repo artifact check for `demo/video.mp4` (if you choose to commit it)
- Duration target window + playback sanity checklist

Do not change `demo/run_demo.py`; this gap is documentation/checklist wiring, not runner behavior.
  </action>
  <verify>rg -n -- "--live --config demo/config.yaml" demo/recording_checklist.md && rg -n "Hosted video URL:|demo/video\\.mp4|duration|playback" demo/submission_checklist.md</verify>
  <done>Checklist live steps are correctly wired to `--live`, and submission checklist contains a final video section (URL + optional file verification).</done>
</task>

</tasks>

<verification>
- `rg -n -- "--live --config demo/config.yaml" demo/recording_checklist.md`
- `rg -n "Hosted video URL:|demo/video\\.mp4" demo/submission_checklist.md`
</verification>

<success_criteria>
- Recording checklist now runs true live mode instead of dry-run fallback.
- Submission checklist includes a final video section with hosted URL and optional `demo/video.mp4` verification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-submit/05-04-SUMMARY.md`
</output>
