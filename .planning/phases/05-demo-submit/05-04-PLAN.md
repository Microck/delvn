---
phase: 05-demo-submit
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - demo/recording_checklist.md
  - demo/submission_checklist.md
  - demo/video.mp4
autonomous: false
gap_closure: true
user_setup:
  - service: azure-cosmos-search
    why: "Credentialed live execution is required to capture real end-to-end demo footage"
    env_vars:
      - name: COSMOS_ENDPOINT
        source: "Azure Portal -> Cosmos DB account -> Keys"
      - name: COSMOS_KEY
        source: "Azure Portal -> Cosmos DB account -> Keys"
      - name: SEARCH_ENDPOINT
        source: "Azure Portal -> Azure AI Search service -> Keys"
      - name: SEARCH_KEY
        source: "Azure Portal -> Azure AI Search service -> Keys"
    dashboard_config:
      - task: "Ensure existing Cosmos containers and Search index from prior phases are available"
        location: "Azure Portal -> Project resources"
must_haves:
  truths:
    - "Recording checklist live steps invoke true live mode (`--live`) with demo config"
    - "A 2-minute submission video exists at `demo/video.mp4` (DEMO-02)"
    - "Submission checklist includes explicit final verification of the video artifact"
  artifacts:
    - path: "demo/recording_checklist.md"
      provides: "Correct live command for recording runbook"
    - path: "demo/video.mp4"
      provides: "Final demo video deliverable for submission"
    - path: "demo/submission_checklist.md"
      provides: "Submission sanity checks including video evidence gate"
  key_links:
    - from: "demo/recording_checklist.md"
      to: "demo/run_demo.py"
      via: "live command in recording steps"
      pattern: "--live --config demo/config.yaml"
    - from: "demo/submission_checklist.md"
      to: "demo/video.mp4"
      via: "final packaging verification"
      pattern: "demo/video\\.mp4"
---

<objective>
Close Phase 05 verification gaps by fixing the live recording command and adding the final demo video artifact.

Purpose: Satisfy DEMO-02 and unblock final submission verification.
Output: Updated recording/submission checklists + recorded `demo/video.mp4`.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-demo-submit/05-demo-submit-VERIFICATION.md
@.planning/phases/05-demo-submit/05-03-SUMMARY.md
@demo/run_demo.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix recording checklist live command and tighten video submission checks</name>
  <files>demo/recording_checklist.md, demo/submission_checklist.md</files>
  <action>
Update `demo/recording_checklist.md` so every live execution instruction uses the real live invocation:
- `python3 demo/run_demo.py --live --config demo/config.yaml`

Also update `demo/submission_checklist.md` to include an explicit final video verification gate (artifact exists, duration window target, and playback sanity) so DEMO-02 is checked before packaging.

Do not change `demo/run_demo.py`; this gap is documentation/checklist wiring, not runner behavior.
  </action>
  <verify>rg -n -- "--live --config demo/config.yaml" demo/recording_checklist.md && rg -n "demo/video\.mp4|duration|playback" demo/submission_checklist.md</verify>
  <done>Checklist live steps are correctly wired to `--live`, and submission checklist contains an explicit video verification gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Record and add final 2-minute demo video artifact</name>
  <what-built>Checklists now point to the correct live command and define final video quality checks.</what-built>
  <action>
In a credentialed environment, record the final demo and add it to the repo:
1. Run `python3 demo/run_demo.py --live --config demo/config.yaml`.
2. Capture the end-to-end flow per `demo/recording_checklist.md`.
3. Export MP4 to `demo/video.mp4` (target 1:50-2:10) and stage it in git.
  </action>
  <how-to-verify>
1. `test -f demo/video.mp4`
2. Confirm playback quality and timing in a media player (clear narration, no secrets, full flow visible).
  </how-to-verify>
  <resume-signal>Type "done" once `demo/video.mp4` is added, or paste blockers if recording could not be completed.</resume-signal>
</task>

</tasks>

<verification>
- `rg -n -- "--live --config demo/config.yaml" demo/recording_checklist.md`
- `test -f demo/video.mp4`
- `rg -n "demo/video\.mp4" demo/submission_checklist.md`
</verification>

<success_criteria>
- Recording checklist now runs true live mode instead of dry-run fallback.
- `demo/video.mp4` exists and is ready for submission review (DEMO-02).
- Submission checklist explicitly verifies the final video artifact before packaging.
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-submit/05-04-SUMMARY.md`
</output>
